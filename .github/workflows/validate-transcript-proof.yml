name: validate transcript proof

on:
  push:
    branches:
      - validate/6175-transcript-proof

defaults:
  run:
    shell: bash

jobs:
  transcript-proof:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: install stack
        uses: unisonweb/actions/stack/install@main
        with:
          stack-version: 3.7.1

      - name: run transcript proof
        run: ./scripts/proofs/transcripts.sh --force

      - name: upload proof file
        uses: actions/upload-artifact@4cec3d8aa04e39d1a68397de0c4cd6fb9dce8ec1
        with:
          name: transcript-proof
          path: .github/workflows/proofs/transcripts.txt
